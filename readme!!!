password -> its a small poem, which seems to be unrelated but some highlighter
letters make out a word that will be the actual password for a file containing 
clues for the other flags, while its a waste of time if they dont figure it out,
the poem is then turned to morse code, the morse code mp3 is then turned to base 64 then the 
link to it is sliced into pieces there will be a hint to find them as all of them and their 
order will be marked in the following matter.

            ↑↑↑
!!!od ot gnihton sah siht¡¡¡

-----------------------------------------------------------------------------------------------------------------
a chain is only as strong as its weakest link

There are keys in the login folder that you must use to find a password
the keys are part of a link and they are marked with a number at the beggining %1 then %2 then %3 and at last %4

run the web page with node app.js
username = Rachidi. good luck finding the rest.

you are free to skip details, enter the database, change the code (at your own risk of losing the flag for ever)
nice regards Team 1.